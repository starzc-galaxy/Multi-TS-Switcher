# 更新日志

## [1.0.0] - 2026-08-20

首个稳定发布版。

### 新增
- 最多 9 组并行，每组最多 9 路 UDP-TS 输入 + 1 路垫片兜底，按配置间隔轮询切换。
- 输入/输出支持单播与组播，可按网卡绑定；不转码，纯 TS 包级转发。
- 关键帧对齐切换 + PCR/PTS/DTS 持续重基准 + discontinuity 标记 + CC 重置。
- 每路源实时监测：无数据超时、CC 错误、PCR 抖动、码率、收包数。
- 响应式监控墙（1-4 列自适应）与 F11 全屏；卡片画面右上角显示组信息/正常异常数/当前源。
- 组配置面板：输入源与实时状态合并表格、网卡下拉选择、配置热生效。
- 离线授权：机器指纹 + Ed25519 签名，授权组数 1-9，支持有效期（天数/永久）与重新导入。
- 生产测试工具：发送 1-9 路彩色测试源、接收验证主程序输出。
- 授权生成器：图形界面签发授权文件（随授权工具携带私钥，需妥善保管）。
- 一键交付打包：Cython 源码防护 + 三程序分别打包 + 使用说明书 docx + 7-Zip 压缩。
- 按模块本地日志、异常退出原因记录、窗口版启动错误落盘。

### 修复
- Windows 多 socket 同端口组播抢包：接收端改按组地址绑定。
- 预览解码 EOF/坏帧导致画面冻结：阻塞读取 + 容错重开 + 切换时重启解码。
- 转发输出 PCR 单位错误与源 PCR 回跳导致下游卡顿：统一 90kHz 基准并自动重对齐。
- Cython 编译槽函数与 PyQt 信号参数不匹配（TypeError）：全部槽函数兼容多余参数。
- 打包后文件对话框不可见：原生对话框打开前强制激活置顶。
